dynamic initializeARTracker(dynamic videoElement) => throw UnsupportedError('AR tracker is web-only');

void setARFilter(String filterName) {}

void stopARTracker() {}

void updateARFilterConfig(
  String filterName,
  double scale,
  double offsetX,
  double offsetY,
) {}
